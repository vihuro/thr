import '../stylesheet/styles.css';
import Button from '../components/UI/button/button_login'

export default function MyApp({ Component, pageProps }) {
    return (

        <Component {...pageProps} />

    )
}